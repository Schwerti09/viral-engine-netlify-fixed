[build]
  command = "npm run build"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[functions]
  node_bundler = "esbuild"
  included_files = [
    "node_modules/.prisma/**",
    "node_modules/@prisma/**",
    "prisma/**"
  ]

[build.environment]
  NODE_VERSION = "20"
